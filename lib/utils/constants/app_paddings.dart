import 'package:flutter/material.dart';

class AppPaddings{
  AppPaddings._();

  static const h42 = EdgeInsets.symmetric(horizontal: 42);
  static const lr22 = EdgeInsets.symmetric(horizontal: 22);
  static const h22v50 = EdgeInsets.symmetric(horizontal: 22,vertical: 50);
}