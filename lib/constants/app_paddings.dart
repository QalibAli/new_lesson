import 'package:flutter/material.dart';

class AppPaddings{
  AppPaddings._();

  static const lr42 = EdgeInsets.symmetric(horizontal: 42);
  static const lr22 = EdgeInsets.symmetric(horizontal: 22);
  static const all42 = EdgeInsets.all(42);
}